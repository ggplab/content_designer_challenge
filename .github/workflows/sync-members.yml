name: Sync Members

on:
  # 1기 종료(2026-05)로 매일 자동 sync 중단. 2기 재개 시 아래 schedule 주석 해제.
  # schedule:
  #   - cron: '0 15 * * *'  # 매일 자정 KST (UTC 15:00)
  workflow_dispatch:       # 수동 실행 가능

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync members from Sheets
        run: node scripts/sync-members.js

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add web/members.json
          if git diff --staged --quiet; then
            echo "변경 없음"
          else
            git commit -m "chore: 멤버 자동 sync ($(TZ=Asia/Seoul date '+%Y-%m-%d'))"
            git push
          fi
