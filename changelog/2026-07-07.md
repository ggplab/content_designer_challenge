# 2026-07-07

시즌2 · 유튜브 에디션 코드 준비 (기획 SSOT: `docs/youtube-challenge-plan.md`). 로컬 커밋까지만 — push/배포/Discord 호출 없음.

## 신규 기능

- **시즌2 URL 정책 — YouTube 롱폼만 인정** (`_shared/platform.ts`)
  - `checkChallengeUrl` 신규: `youtube.com/shorts/` URL 거부(reason=`shorts`), YouTube 외 플랫폼 거부(reason=`not_youtube`), watch/`youtu.be`만 인정
  - `isShortsUrl` 신규 (`m.youtube.com` 등 서브도메인 포함, 대소문자 무관)
  - `detectPlatform` 시그니처·동작 유지 (web-verify 등 기존 사용처 영향 없음)
- **스트릭(🔥 연속 N주) 도입 — 메달 🥇🥈🥉 제거** (`_shared/week.ts`, `_shared/platform.ts`)
  - `getMedal` 제거 → `parseWeekNumber` / `calcStreak` / `formatStreakBadge` 추가
  - 스트릭 = 기준 주 포함 연속으로 주 1회 이상 인증한 주 수 (기준 주 미인증이면 0)
  - `discord-verify/services/sheets.ts`: `getWeekCounts`가 유저 인증 주차 집합(`userWeeks`) 함께 반환
- **인증 응답 개편** (`discord-verify/verification.ts`)
  - 거부 링크는 사유별 안내 (쇼츠 / YouTube 외 플랫폼), 인정 가능 링크가 0개면 가이드와 함께 전체 거부
  - 인증 완료 메시지에 스트릭 배지 표시, 메달 제거
  - 같은 주 중복 제출 시 "주간 인정은 1회" 안내 (기록은 저장)

## 변경사항

- **주차 시작일 시즌2로 교체** (`_shared/week.ts`) — `PUBLISH_START_DATE = "2026-07-13"` 단일 상수로 구조화. 시작일 변경 시 이 상수와 `web/index.html`의 `CHALLENGE_START` 두 곳만 수정 (상호 참조 주석 명시)
- **`config/challenge_config.json` 재생성** — 시즌2 기간(준비 07-07~07-12 · 발행 07-13~10-04 12주)·주 1회 규칙·URL 정책 문서화. 참가자 명단은 `web/members.json` SSOT 유지
- **주간 정산 포맷 단순화** (`weekly-summary/summary.ts`)
  - 멤버별 `✅ 달성 · 🔥 N주 연속` / `❌ 미달성` 중심으로 개편 — 명단은 `web/members.json`을 GitHub Pages에서 읽음 (`MEMBERS_JSON_URL` env로 재정의 가능, 실패 시 시트 기록 기반 fallback)
  - Top 3 메달 표기 제거, Gemini 추천 콘텐츠 선정은 유지 (중복 선정 시 dedupe)
  - 집계 로직을 순수 함수로 분리 export (`buildUserWeeks`, `computeAchievements`, `filterCandidates`, `fetchRoster`) — 테스트가 실제 모듈을 import
- **웹 대시보드 시즌2 전환** (`web/`)
  - `members.json`: 임정·슬기 2인 등록 (표기명 추후 조정 가능, `nickname_map`은 새 서버 닉네임 확인 후 채움)
  - 시즌1 종료 배너 → 시즌2 안내 배너, 인증 버튼(verify-cta) 복원 — API 키 버튼(api-cta)은 OAuth 비활성 유지 방침으로 미복원 (`docs/season1-pause.md` §3)
  - `CHALLENGE_START` 2026-07-13, gviz 조회에 `SHEET_TAB` 상수 도입
  - 시즌1 설문 스냅샷 병합 중단 (함수는 보존), 프로필 링크 없을 때 기본 카드 fallback

## 버그 수정

- **`weekly-summary`의 깨진 `shortenUrl` import 제거** — 2026-04-14 `_shared/url.ts` 교체(`shortenUrl` → `generateShortCode`) 때 `summary.ts`에 미반영되어 있던 잠재 타입 오류. 추천 콘텐츠 링크는 원본 URL 사용으로 정리

## 이력 정리

- **bugs/open 001·002 → bugs/closed 이동** — changelog 기준 이미 해결(001: 2026-04-14 자체 URL 단축, 002: 2026-03-19 weekly-summary). 각 파일에 해결 근거 1줄 추가

## 테스트

- 시즌2 기준 전면 갱신: 주차 계산(07-13 기점), `checkChallengeUrl`/`isShortsUrl`, `calcStreak`/`parseWeekNumber`/`formatStreakBadge`, `getWeekCounts.userWeeks`, weekly-summary 집계(달성/미달성·스트릭·roster fallback)
- `/unit-test` 스킬 스크립트·문서 동기화 (메달 → 스트릭)
- **전체 157개 테스트 통과** (`deno test tests/ --allow-all --ignore="tests/dashboard-data.test.js"`)

## 배포 전 남은 수동 단계 (재가동 runbook: `docs/season1-pause.md` + 기획안 §6)

1. 시즌2 전용 Google Sheets 탭(또는 새 시트) 생성 — 시즌1 행과 주차 레이블("N주차")이 겹치므로 분리 필수
2. Supabase Secrets: `GOOGLE_SHEET_TAB`(신규 탭명), `DISCORD_GUILD_ID`·`DISCORD_WEEK_SUMMARY_CHANNEL_ID`(새 서버) 갱신
3. `web/index.html`의 `SHEET_TAB` 상수를 신규 탭명으로 교체
4. web/ push (GitHub Pages — members.json이 공개되어야 weekly-summary 명단 로드 가능) → Edge Function 배포 (`discord-verify`, `weekly-summary`)
5. `/인증` 슬래시 커맨드 새 길드 등록, pg_cron 2개 재등록 (워밍업·주간 정산)
6. 리허설: `/인증` 1건 (watch URL 인정 + shorts URL 거부 확인) → Sheets 행·follow-up·갤러리 확인, 주간 정산 수동 트리거 1회
