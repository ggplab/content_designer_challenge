# 2026-04-14

## 버그 수정

- **비공개 제출 시 메달 이모지 누락 수정** — 비공개 인증 메시지에서 플랫폼명만 표시되고 🥇🥈🥉 메달이 빠지던 문제 수정 (`discord-verify/verification.ts`)

## 신규 기능

- **URL 단축 기능 자체 구현으로 전환** — 외부 API(is.gd) 의존성 제거, Supabase DB 기반으로 자체 URL 단축 서비스 구축
  - `short_links` 테이블 신규 생성 (`code` PK, `original_url`, `created_at`) — migration `20260414000000_create_short_links.sql`
  - `r` Edge Function 신규 추가 — `/{code}` 요청 수신 후 DB 조회 → 원본 URL로 301 리다이렉트
  - `_shared/short-links.ts` 신규 추가 — `createShortLink` (충돌 시 최대 5회 재시도), `resolveShortLink`
  - `verification.ts`: Sheets column H(shortCode) 제거 → Supabase DB insert로 전환
  - URL 단축 실패 시 원본 URL fallback 유지 (Sheets 저장 및 Discord 메시지 정상 처리)

## 변경사항

- **`_shared/url.ts` 교체** — `shortenUrl`(외부 API 호출) 제거 → `generateShortCode` (랜덤 6자리 코드 생성)로 교체
- **`_shared/supabase.ts`** — `getSupabaseServiceRoleKey()`에 `SUPABASE_SERVICE_ROLE_KEY` 자동 주입 fallback 추가 (로컬 `supabase start` 환경 대응)
- **Google Sheets 컬럼 변경** — column H(`shorten_url`) 제거, 인증 기록은 A~G 컬럼만 사용
- **`REDIRECT_BASE_URL` env 추가** — 로컬 개발 시 ngrok URL, 운영기는 `SUPABASE_URL` fallback

## 개발환경

- **로컬 Supabase 개발 환경 구성** — `supabase/config.toml` 초기화
  - `discord-verify`, `r` 함수에 `verify_jwt = false` 설정 (운영기 배포 설정과 동일)
  - `supabase/.gitignore`에 `functions/.env` 추가 (로컬 시크릿 보호)

## 테스트

- **`tests/shared/short-links.test.ts` 신규** — `createShortLink` (성공/충돌 재시도/재시도 초과/기타 오류), `resolveShortLink` (조회 성공/없는 코드) 6개 케이스
- **`tests/shared/supabase.test.ts` 보완** — `SUPABASE_SERVICE_ROLE_KEY` fallback 테스트 2개 추가
- **전체 129개 테스트 통과**
